# Install on your PC/laptop for training the model
# Usage: pip install -r requirements_training.txt
# If you are using MacOS, Ubuntu 23.04+ or any other modern Linux distribution, you may need 
# to run these requirements in a virtual environment:
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r requirements_training.txt
#
# If you still encounter issues (ESPECIALLY with tensorflow), refer to the README for
# documentation or try installing the packages individually:
#   pip install --upgrade pip
#   pip install mediapipe tensorflow mediapipe-model-maker numpy matplotlib opencv-python

pyyaml>=6.0 
mediapipe>=0.8.0
tensorflow>=2.10.0
mediapipe-model-maker
numpy
matplotlib>=3.0
opencv-python>=4.5.0